# Backend REST base URL (used for /api/* calls)
# VITE_BACKEND_HTTP_URL=http://localhost:8000

# Backend WebSocket URL (used for real-time game events)
# VITE_BACKEND_WS_URL=ws://localhost:8000/ws/wordduel/

